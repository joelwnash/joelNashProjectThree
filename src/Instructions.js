const Instructions = () => {
    return(
        <div className="instructionsSection">
            <p className="instructions">The Background Noise Generator will produce a background image for you. Enter values for the inputs below, or click the button to generate your image without entering inputs to get a randomized colour.</p>
        </div>
    )
}

export default Instructions;